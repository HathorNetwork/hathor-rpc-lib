# Logging level (default: warn)
# Options: debug, info, warn, error
VITE_LOG_LEVEL=warn

# Minimum required Snap version (semantic versioning)
# Users with older versions will be prompted to update
# Default: 0.0.0 (accepts any version)
VITE_MIN_SNAP_VERSION=0.3.0

# Snap origin (default: local:http://localhost:8080)
# For production: npm:@hathor/snap
# For development: local:http://localhost:8080
SNAP_ORIGIN=local:http://localhost:8080
